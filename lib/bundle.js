
/* This file just bundles modules to a single default export for IIFE, instead of named exports */

import {batchAnimationFrame, each, element, filter, inherits, map, shuffle, renderer, Observable, SVGelement, View, Views} from './index'

export default {batchAnimationFrame, each, element, filter, inherits, map, shuffle, renderer, Observable, SVGelement, View, Views}
