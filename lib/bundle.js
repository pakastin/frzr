
import {batchAnimationFrame, each, element, filter, inherits, map, shuffle, renderer, SVGelement, View, Views} from './index'

export default {batchAnimationFrame, each, element, filter, inherits, map, shuffle, renderer, SVGelement, View, Views}
