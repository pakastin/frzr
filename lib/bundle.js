
import {batchAnimationFrame, each, element, filter, fisheryates, inherits, map, renderer, SVGelement, View, Views} from './index'

export default {batchAnimationFrame, each, element, filter, fisheryates, inherits, map, renderer, SVGelement, View, Views}
