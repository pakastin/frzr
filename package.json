{
  "name": "frzr",
  "version": "0.16.1",
  "description": "Turboboosted 2 KB view library",
  "main": "dist/frzr.js",
  "jsnext:main": "src/index.js",
  "scripts": {
    "dev": "node watch",
    "deploy": "gh-pages -d dist",
    "build": "npm run test && rollup -n frzr -f umd src/index.js -o dist/frzr.js && npm run uglify",
    "uglify": "uglifyjs dist/frzr.js -cmo dist/frzr.min.js",
    "postversion": "git push --tags",
    "prepublish": "npm test",
    "postpublish": "npm run deploy",
    "test": "rollup -n frzr -f cjs src/index.js -o test/frzr.js && browserify -t coverify test/test.js | tape-run | coverify",
    "justtest": "browserify -t coverify test/test.js | tape-run | coverify"
  },
  "author": "Juha Lindstedt",
  "license": "ISC",
  "devDependencies": {
    "browserify": "~13.0.0",
    "chokidar-cli": "~1.2.0",
    "coverify": "~1.4.1",
    "frzr-dom": "0.0.1",
    "gh-pages": "~0.11.0",
    "rollup": "~0.25.4",
    "tape": "~4.5.1",
    "tape-run": "~2.1.3",
    "uglify-js": "~2.6.2"
  }
}
